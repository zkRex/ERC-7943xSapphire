# Private key for deploying to Sapphire mainnet or testnet
# Required only if deploying to sapphire or sapphire-testnet networks
# DO NOT commit your actual private key to version control
PRIVATE_KEY=

# Local Sapphire node URL
# Optional: defaults to http://localhost:8545
# Override this if your local Sapphire node is running on a different URL
LOCALNET_URL=http://localhost:8545

# uRWA20 Deployment Parameters
# These are used when deploying the uRWA20 contract

# Token name (e.g., "Real World Asset Token")
TOKEN_NAME=Real World Asset Token

# Token symbol (e.g., "RWA")
TOKEN_SYMBOL=RWA

# Initial admin address (receives all roles: ADMIN, MINTER, BURNER, FREEZING, WHITELIST, FORCE_TRANSFER, VIEWER)
# Leave empty to use the deployer's address
INITIAL_ADMIN=

# SIWE (Sign-In With Ethereum) domain for authentication
# For testnet: use your testnet domain (e.g., "testnet.example.com")
# For mainnet: use your production domain (e.g., "app.example.com")
# For localnet: defaults to "localhost"
SIWE_DOMAIN=localhost
